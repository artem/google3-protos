syntax = "proto2";

package research_scam;

option java_multiple_files = true;

message DenseStatsConfig {
    optional bool enabled = 1 [default = false];
    optional string output_location = 2;
    optional int32 num_correlations = 3 [default = 20];
}
