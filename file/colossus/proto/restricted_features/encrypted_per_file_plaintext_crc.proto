syntax = "editions";

package colossus.restricted_features;

message EncryptedPerFilePlaintextCrc {
    optional bool enabled = 1;
}

message PerFilePlaintextCrcParallel {
    optional bool enabled = 1;
}
