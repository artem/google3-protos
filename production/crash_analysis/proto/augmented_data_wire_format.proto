syntax = "editions";

package coroner.collection;

enum AugmentedDataWireFormat {
    AUGMENTED_DATA_WIRE_FORMAT_UNSPECIFIED = 0;
    RAW = 1;
    RECORDIO = 2 [deprecated = true];
    RECORDIO_PROTOBUILDER = 3;
}
