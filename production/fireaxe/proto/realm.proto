syntax = "proto3";

package fireaxe.config_realm;

option java_multiple_files = true;
option optimize_for = CODE_SIZE;
option java_package = "com.google.protos.fireaxe.configrealm";

enum Realm {
    DEFAULT = 0;
    PROD = 1;
    DEV = 4;
    TEST = 2;
    INSECURE_HERMETIC_TEST = 5;
    INSECURE_FROM_FLAGS_FOR_TESTING_ONLY = 3;
}
