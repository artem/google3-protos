syntax = "proto2";

package voxel;

enum WindowFrameType {
    WINDOW_FRAME_INVALID = 0;
    WINDOW_FRAME_ROWS = 1;
    WINDOW_FRAME_RANGE = 2;
}

enum WindowFrameBoundaryType {
    WINDOW_FRAME_BOUNDARY_INVALID = 0;
    WINDOW_FRAME_BOUNDARY_UNBOUNDED = 1;
    WINDOW_FRAME_BOUNDARY_OFFSET_PRECEDING = 2;
    WINDOW_FRAME_BOUNDARY_CURRENT_ROW = 3;
    WINDOW_FRAME_BOUNDARY_OFFSET_FOLLOWING = 4;
}
